# EHRGym training dependencies (TRL GRPO)
# Install on GPU box:
#   pip install -r requirements-train.txt
#
# For vLLM acceleration:
#   pip install -r requirements-train.txt "trl[vllm]"

torch>=2.4.0
trl>=0.29.0
transformers>=4.45.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
bitsandbytes>=0.44.0
httpx>=0.28.1
wandb           # optional, for logging

# Legacy Unsloth support (optional)
# unsloth
# xformers

# EHRGym training dependencies (Unsloth)
# Install on H100 box:
#   pip install -r requirements-train.txt
#
# Or the bleeding-edge Unsloth from git:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

torch>=2.4.0
unsloth
xformers
trl>=0.12.0
datasets>=3.0.0
bitsandbytes>=0.44.0
wandb           # optional, for logging
httpx           # needed for GRPO script to call EHRGym env server
